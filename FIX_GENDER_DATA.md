# إصلاح بيانات Gender في قاعدة البيانات

## 🔴 المشكلة:
```
Value '' not found in enum 'Gender'
```

هناك مستخدمون في قاعدة البيانات بقيمة `gender` فارغة (`''`) بدلاً من `null` أو قيمة صحيحة.

## ✅ الحل:

### الخطوة 1: إصلاح البيانات الموجودة
نفذ الأمر التالي لإصلاح جميع المستخدمين الذين لديهم `gender` غير صحيح:

```bash
cd Backend
npm run prisma:fix:gender
```

أو مباشرة:
```bash
cd Backend
node prisma/fix_gender.js
```

هذا سيقوم بـ:
- البحث عن جميع المستخدمين الذين لديهم `gender` فارغ أو غير صحيح
- تحديثهم إلى `null` (حقل اختياري)

### الخطوة 2: تشغيل Seed
بعد إصلاح البيانات، يمكنك تشغيل seed:

```bash
cd Backend
npm run prisma:seed
```

## 📝 ملاحظات:

- Script الإصلاح سيعيّن `gender` إلى `null` للمستخدمين الموجودين
- عند تشغيل seed، سيتم تحديث `gender` للمستخدمين الموجودين بناءً على بيانات seed
- المستخدمون الجدد سيحصلون على `gender` صحيح من seed

## 🔍 التحقق:

بعد إصلاح البيانات، يمكنك التحقق باستخدام:
```bash
npm run prisma:studio
```

افتح جدول `users` وتحقق من أن جميع `gender` إما `null` أو `MALE` أو `FEMALE`.































