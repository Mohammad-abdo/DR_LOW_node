# تحديث مدة صلاحية Access Token

## ✅ التحديث المطبق:

تم تحديث مدة صلاحية Access Token من **15 دقيقة** إلى **10 أيام**.

### الملف المحدث:
- `Backend/src/utils/jwt.js`

### التغيير:
```javascript
// قبل:
expiresIn: process.env.JWT_EXPIRES_IN || '15m',  // 15 دقيقة

// بعد:
expiresIn: process.env.JWT_EXPIRES_IN || '10d',  // 10 أيام
```

## 🔧 Environment Variable:

يمكنك تخصيص المدة عبر environment variable في `.env`:
```env
JWT_EXPIRES_IN=10d
```

### صيغ المدة المدعومة:
- `10d` - 10 أيام
- `240h` - 240 ساعة (10 أيام)
- `14400m` - 14400 دقيقة (10 أيام)
- `864000s` - 864000 ثانية (10 أيام)

## 📋 ملاحظات:

1. **Security Consideration**: 
   - زيادة مدة صلاحية access token تقلل من الحاجة لتجديده
   - لكنها قد تزيد من المخاطر الأمنية إذا تم سرقة الـ token
   - يُنصح باستخدام refresh token للتجديد الآمن

2. **Refresh Token**:
   - مدة صلاحية refresh token تبقى **7 أيام** (افتراضي)
   - يمكن استخدام refresh token لتجديد access token عند انتهاء صلاحيته

3. **Best Practice**:
   - للـ production، يُنصح باستخدام access token قصير المدى (15m - 1h)
   - واستخدام refresh token لتجديده تلقائياً
   - لكن للمرونة، يمكن تخصيص المدة حسب الحاجة

## 🧪 Testing:

بعد التحديث:
1. سجّل الدخول للحصول على access token جديد
2. استخدم الـ token في الطلبات
3. الـ token سيبقى صالحاً لمدة 10 أيام

## ⚠️ Important:

- بعد التحديث، يجب إعادة تشغيل الـ server
- جميع الـ tokens الجديدة ستكون صالحة لمدة 10 أيام
- الـ tokens القديمة (15 دقيقة) ستبقى صالحة حتى انتهاء صلاحيتها































