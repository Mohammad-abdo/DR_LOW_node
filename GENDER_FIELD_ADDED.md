# إضافة حقل Gender (الجنس) للتسجيل

## ✅ التغييرات المكتملة:

### Backend:
1. ✅ تم إضافة enum `Gender` في `schema.prisma` مع القيم: `MALE`, `FEMALE`
2. ✅ تم إضافة حقل `gender` إلى نموذج `User` (اختياري)
3. ✅ تم تحديث `registerStudent` في `authController.js`:
   - قبول حقل `gender` من الطلب
   - إضافة validation للـ gender (يجب أن يكون MALE أو FEMALE)
   - حفظ gender في قاعدة البيانات
   - إرجاع gender في استجابة `getMe`

### Frontend:
1. ✅ تم إضافة `gender` إلى `formData` state
2. ✅ تم إضافة حقل Select للـ gender في نموذج التسجيل
3. ✅ تم إرسال `gender` في API request

## 📝 الخطوة المتبقية (مهمة):

### تشغيل Migration:

بسبب قيود PowerShell، يجب تشغيل الأمر التالي يدوياً:

```bash
cd Backend
npx prisma migrate dev --name add_gender_to_user
```

أو إذا كنت تستخدم Git Bash:

```bash
cd Backend
npx prisma migrate dev --name add_gender_to_user
```

بعد تشغيل Migration، سيتم:
- إضافة عمود `gender` إلى جدول `users` في قاعدة البيانات
- إنشاء enum `Gender` في قاعدة البيانات

## 🧪 اختبار:

1. افتح صفحة التسجيل: `http://localhost:5173/student/register`
2. املأ النموذج واختر الجنس من القائمة المنسدلة
3. أكمل التسجيل وتحقق من نجاح العملية
4. تحقق من أن الجنس تم حفظه في قاعدة البيانات

## 📋 ملاحظات:

- حقل `gender` اختياري (optional) - يمكن للطالب التسجيل بدون تحديد الجنس
- القيم المقبولة: `MALE` أو `FEMALE`
- يتم عرض الجنس في واجهة المستخدم بالعربية والإنجليزية






















